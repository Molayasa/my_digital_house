/**
 * DH-Pelis
 * 
 * El Tech Leader presenta al equipo un nuevo proyecto de compra y venta de películas. 
 * Quiere que mostremos que tenemos claro cómo hacer para exportar e importar nuestros propios módulos.
 * Para ello, seguiremos los siguientes pasos:
 * 
 * a. Crear una carpeta llamada proyecto.
 * b. Dentro de ella crear un archivo llamado peliculas.js. 
 * Este archivo será un módulo que contiene una lista con algunas de nuestras películas favoritas.
 * 
 * Una vez creado el módulo anterior, crear un nuevo archivo (app.js). Piensa cómo hacer para lograr importar nuestro módulo (peliculas.js). 
 * Luego, utilizando lo que sabemos sobre ciclos o bucles, muestra al usuario un listado con el detalle de cada una de nuestras películas favoritas.
 * 
 */
